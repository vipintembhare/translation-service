spring:
  application:
    name: client-config
  cloud:
    config:
     uri: http://config-server-service.dev:8888/config-server
  profiles:
    active: default
        
management:
  security:
    enabled: false
    
server:
  port: 8080
